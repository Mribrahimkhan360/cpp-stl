#include<bits/stdc++.h>
using namespace std;
int main()
{
	vector<int>v,t;
	v.push_back(1);
	v.push_back(2);
	v.push_back(3);
	v.push_back(4);
	t=v;//coping
	for (int i=0; i<t.size();i++)
	{
		cout<<t[i]<<endl;
		/* code */
	}
    return 0;
}